# Android native client

Open this directory in Android Studio and run the `app` configuration on an Android emulator or connected phone.

The app uses Kotlin, Jetpack Compose, coroutines and the platform HTTP client. The node address must be reachable from the phone, for example `http://192.168.1.100:5284`.
